"""Adjacent-artist discovery (plan 01): offline enrichment scripts for the
collab-frontier crawl. The crawl itself lives in the normal pipeline
(requests_factory seeding + response handlers); the ranked-discovery query
pack lives in application/graph_database/queries/discovery/."""
